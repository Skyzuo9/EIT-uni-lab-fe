# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

Uni-Lab 面向实验室自动化研发、设备集成和系统运维人员。核心用户在实验室或开发环境中连接和控制仪器设备、管理物料与空间关系、编排实验工作流，并依据真实运行状态定位和处理问题。

## Product Purpose

Uni-Lab 是统一的实验室自动化工作台。它将仪器设备控制、物料管理、2D/2.5D/3D 实验室场景、工作流编排和运行调试汇集在同一个操作界面中，帮助用户从实验建模走到可靠执行。

成功意味着用户能够清楚识别当前系统和设备状态，准确完成高风险操作，理解物料与工作流之间的关系，并在异常发生时获得可追溯、可行动的信息。

## Positioning

Uni-Lab 不把设备控制、物料场景和实验流程拆成互不相干的工具。它以统一领域投影、稳定标识和权威运行状态连接这些工作，使同一实验事实能够在控制、空间和流程视图中保持一致。

## Operating Context

- 浏览器应用与 Electron 桌面端共用 `apps/kernel-web` 这一套 renderer。
- 用户通过 Bohrium 账号登录，在工作台中切换仪器设备、物料、3D 场景和工作流模块。
- 系统可连接本地 Go 服务、本地 Python OS 或 Uni-Lab Cloud；业务界面不依赖具体部署形态。
- 实验室工作台同时涉及设备动作、物料聚合、空间坐标、3D 模型、Canonical 工作流、运行事件和结构化诊断。
- 用户需要在长时间、高信息密度和可能光线较暗的实验室或调试环境中快速读取状态并采取行动。

## Capabilities and Constraints

- `apps/kernel-web` 是唯一长期维护的 Web renderer；Electron 仅提供桌面外壳，不创建第二套界面。
- 仪器设备模块当前先提供机器人和摄像头的本地控制展示，不依赖 Edge 端上报；设备名称不添加 PTLC 前缀。
- 物料、工作流和 Pascal 场景各有唯一状态所有者，组合层只传递稳定 ID、选择、高亮和命令意图。
- 后端或 OS 是运行状态、节点结果、设备反馈和异常的权威来源。界面不得伪造成功、失败、暂停或运行终态。
- 工作流以 Canonical revision 为唯一事实源；JSON、Python、DAG 和调试视图必须保持可追溯映射。
- 所有持久坐标使用毫米，旋转使用 XYZ degree；渲染器不得猜测坐标系。
- 高频实时状态只更新必要的可视化投影，不得反向改写稳定领域数据。
- 产品当前以中文操作界面为主，技术标识、协议名称和代码内容可保留英文。

## Brand Commitments

- 产品名称为 **Uni-Lab**。
- 产品语气应冷静、准确、克制，优先说明事实、状态、影响和下一步操作。
- 术语必须在设备、物料、场景和工作流模块之间保持一致，不用营销语言替代领域概念。
- 产品界面服务于任务完成；品牌表达不能降低信息密度、可读性或操作确定性。

## Evidence on Hand

- 仓库架构与状态所有权：`AGENTS.md`、`docs/architecture.md`。
- 产品入口与应用边界：`README.md`、`apps/kernel-web/README.md`。
- 已实现的统一应用外壳与模块导航：`apps/kernel-web/src/components/AppShell.tsx`。
- 已实现的设计 token：`packages/design-system/src/theme.css`。
- 已实现的仪器设备控制界面：`apps/kernel-web/src/components/device/DevicePanel.tsx`。
- 已实现的设备 E2E 验证：`e2e/device-controls.spec.ts`。
- 当前没有已确认的客户评价、商业指标或合规认证；未来界面与文档不得虚构这些证据。

## Product Principles

1. **权威状态优先。** 清楚区分已确认事实、进行中状态、未知状态和前端预览。
2. **一个事实，一个所有者。** 所有视图从同一领域事实派生，避免复制和漂移。
3. **高风险操作必须明确。** 设备动作、运行控制和紧急操作要有清晰范围、反馈与防误触机制。
4. **复杂系统仍需快速理解。** 通过稳定层级、术语和跨视图关联降低排查成本，而不是隐藏必要信息。
5. **降级必须诚实。** 离线、能力缺失、实时连接中断和局部失败都应以可行动的信息呈现。

## Accessibility & Inclusion

- 产品界面至少满足 WCAG 2.1 AA。
- 颜色不能成为状态的唯一表达；状态同时通过文字、图标、结构或可访问标签传达。
- 所有核心操作必须支持键盘访问并具有清晰的焦点状态。
- 尊重用户的减少动态效果设置，避免无必要的持续动画和可能干扰精确操作的动效。
- 高信息密度不能以牺牲可读性为代价；正文、状态、数值和代码内容应保持足够对比度与字号。
