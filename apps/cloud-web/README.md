# Cloud Web

Reserved for a future cloud-specific entry point. The current product has one
renderer in `apps/kernel-web`; this directory intentionally has no
`package.json` and does not participate in workspace builds.
