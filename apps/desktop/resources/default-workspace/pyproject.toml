[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "unilab-bundled-reference"
version = "1.0.0"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["unilab_bundled_reference*"]

